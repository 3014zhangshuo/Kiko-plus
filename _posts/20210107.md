浏览器渲染页面分两个步骤：
  + page-building (two steps and their can switch)
    + processing DOM
    + executing JavaScript code (when encounter script tag)
  + event-handling

JavaScript code
  + function code
  + global code
